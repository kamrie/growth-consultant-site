import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        accent: "#3B82F6",
        accentLight: "#60A5FA",
        accentDark: "#2563EB",
      },
    },
  },
};

export default config;
